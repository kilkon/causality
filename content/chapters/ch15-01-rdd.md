---
title: 13.1 배정변수와 임계값의 역할
description: RDD는 배정변수 \(R\)가 임계값 \(c\)를 넘는지 여부로 처치가 결정될 때, \(c\) 근방 단위들 사이의 결과 불연속을 인과효과로 해석하는 설계다. 핵심은 “임계값 바로 좌우는 잠재결과가 연속”이라는 가정이다.
---

# 13.1 배정변수와 임계값의 역할

RDD는 배정변수 \(R\)가 임계값 \(c\)를 넘는지 여부로 처치가 결정될 때, \(c\) 근방 단위들 사이의 결과 불연속을 인과효과로 해석하는 설계다. 핵심은 “임계값 바로 좌우는 잠재결과가 연속”이라는 가정이다.

## 13.1 배정변수와 임계값의 역할
회귀불연속 설계(regression discontinuity design, RDD)를 이해하는 가장 좋은 출발점은 “점수 하나가 정책을 갈라놓는 상황”을 떠올리는 것이다. 시험점수, 소득인정액, 연령, 위험지수, 대기기간 같은 수치가 기준선을 넘으면 처치를 받고, 넘지 못하면 받지 못한다. 정책평가에서 이런 제도는 매우 흔하다.

## 먼저 RDD 개념부터: 무엇을 비교하는 설계인가

RDD의 기본 구조는 다음과 같이 쓸 수 있다.

\[
D_i=\mathbb{1}[R_i \ge c]
\]

여기서

- \(R_i\): 배정변수(running variable)
- \(c\): 임계값(cutoff)
- \(D_i\): 실제 처치 여부

이다. 즉 개인 \(i\)의 처치 여부는 \(R_i\)가 \(c\)를 넘는지 여부에 의해 제도적으로 결정된다.

이때 RDD의 핵심 질문은 “전체 표본에서 누가 더 비슷한가”가 아니다. 오히려 **임계값 바로 위와 바로 아래의 단위들은 제도 규칙만 빼면 거의 같은가**를 묻는다. 따라서 RDD는 전표본 평균 비교가 아니라 **경계 근방의 국소 비교(local comparison)**를 이용하는 설계다.

## 수식으로 보면 무엇을 식별하는가

RDD에서 관측되는 결과는

\[
Y_i = D_iY_i(1) + (1-D_i)Y_i(0)
\]

로 쓸 수 있다. sharp RDD에서 cutoff에서의 인과효과는 보통 다음처럼 정의된다.

\[
\tau_{SRD}(c)
=
\lim_{r\downarrow c}\mathbb{E}[Y\mid R=r]
-
\lim_{r\uparrow c}\mathbb{E}[Y\mid R=r]
\]

이 식은 임계값 바로 오른쪽의 평균 결과와 왼쪽의 평균 결과 차이를 뜻한다. 그런데 이 차이를 인과효과로 읽으려면, 처치가 없었다면 결과함수가 cutoff에서 매끈하게 이어졌을 것이라는 연속성 가정이 필요하다.

\[
\lim_{r\downarrow c}\mathbb{E}[Y(0)\mid R=r]
=
\lim_{r\uparrow c}\mathbb{E}[Y(0)\mid R=r]
\]

\[
\lim_{r\downarrow c}\mathbb{E}[Y(1)\mid R=r]
=
\lim_{r\uparrow c}\mathbb{E}[Y(1)\mid R=r]
\]

이 가정이 설득력 있다면, 관측된 결과의 점프는 처치 때문이라고 해석할 수 있다. 즉 RDD는 “연속적이어야 할 결과가 cutoff에서 갑자기 끊기면, 그 불연속은 제도적 처치 때문에 생긴 것”이라는 논리를 사용한다.

## 시각적으로 보면 무엇을 보는가

<figure class="figure">
  <svg viewBox="0 0 940 360" role="img" aria-label="RDD의 기본 개념과 cutoff에서의 점프">
    <defs>
      <marker id="arr-rdd-131" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#8f3b1b"></path>
      </marker>
    </defs>
    <rect x="70" y="35" width="800" height="250" rx="18" fill="#fffdf8" stroke="#e9d7c3"/>
    <line x1="120" y1="250" x2="820" y2="250" stroke="#6b5d52" stroke-width="2.2"/>
    <line x1="120" y1="250" x2="120" y2="80" stroke="#6b5d52" stroke-width="2.2"/>
    <text x="470" y="300" text-anchor="middle" font-size="18" fill="#4b5563">배정변수 \(R\)</text>
    <text x="72" y="165" text-anchor="middle" font-size="18" fill="#4b5563" transform="rotate(-90 72 165)">결과 \(Y\)</text>

    <line x1="500" y1="85" x2="500" y2="250" stroke="#c2410c" stroke-width="3" stroke-dasharray="10 7"/>
    <text x="500" y="72" text-anchor="middle" font-size="18" fill="#9a3412">cutoff \(c\)</text>

    <path d="M150 220 C250 205, 330 185, 485 170" fill="none" stroke="#2563eb" stroke-width="4"/>
    <path d="M515 135 C610 125, 690 105, 790 92" fill="none" stroke="#16a34a" stroke-width="4"/>

    <circle cx="485" cy="170" r="6" fill="#2563eb"/>
    <circle cx="515" cy="135" r="6" fill="#16a34a"/>
    <line x1="485" y1="170" x2="515" y2="135" stroke="#b91c1c" stroke-width="4" marker-end="url(#arr-rdd-131)"/>

    <text x="305" y="190" text-anchor="middle" font-size="16" fill="#1d4ed8">cutoff 왼쪽의 평균 결과</text>
    <text x="685" y="118" text-anchor="middle" font-size="16" fill="#15803d">cutoff 오른쪽의 평균 결과</text>
    <text x="555" y="160" font-size="16" fill="#b91c1c">불연속 크기 = 국소 효과</text>
    <text x="470" y="334" text-anchor="middle" font-size="15" fill="#6b5d52">핵심은 cutoff에서의 “점프”를 읽는 것이다.</text>
  </svg>
  <figcaption>RDD는 결과함수 전체를 비교하는 것이 아니라, cutoff 바로 좌우에서 발생하는 불연속을 읽는다. 이 점프가 sharp RDD의 핵심 estimand다.</figcaption>
</figure>

이 그림에서 중요한 것은 두 가지다.

1. cutoff에서 멀리 떨어진 관측치들은 서로 꽤 다를 수 있다.
2. 하지만 cutoff 바로 근처에서는 좌우 단위들이 매우 비슷하고, 제도 규칙 때문에 처치 여부만 달라질 수 있다.

따라서 RDD의 직관은 “넓게 보면 다른 집단이지만, 경계 바로 근처에서는 비교 가능한 단위들”이라는 데 있다.

회귀불연속 설계(RDD)의 출발점은 매우 제도적이다. 어떤 정책이나 프로그램에서 개인의 처치 여부가 시험점수, 소득, 연령, 위험지수 같은 **배정변수(running variable)** \(R_i\)가 특정 임계값 \(c\)를 넘는지에 따라 달라질 때, cutoff는 사실상 처치를 켜고 끄는 스위치가 된다.

\[
D_i=\mathbb{1}[R_i\ge c]
\]

이 규칙이 엄격히 적용되면, \(R_i\)가 \(c\) 바로 위인 사람과 바로 아래인 사람은 매우 비슷하지만 처치 여부만 달라질 수 있다. RDD는 바로 이 “경계 근방의 비교”를 이용한다. 그래서 RDD는 전체 표본 평균 효과가 아니라 \(R=c\) 근방의 **국소 효과(local effect)**를 목표로 한다.

## 배정변수는 단순한 설명변수가 아니다

RDD에서 배정변수 \(R\)는 보통 회귀의 통제변수처럼 취급되지 않는다. 오히려 설계 전체를 규정하는 축이다. 시험성적, 자산점수, 빈곤지수, 위험점수, 생년월일, 연령, 대기기간처럼 실제 제도에서 처치 배정에 쓰인 연속적 또는 준연속적 지표가 여기에 해당한다.

정책평가 사례를 보면 배정변수의 역할이 더 분명해진다.

- 장학금 제도: 성적 \(R\)가 cutoff를 넘으면 장학금 수혜 \(D\)
- 복지급여: 소득인정액 \(R\)가 기준 이하이면 수급 \(D\)
- 교육지원: 시험점수 \(R\)가 기준 이상이면 특별반 배정 \(D\)
- 보건정책: 위험점수 \(R\)가 기준 이상이면 집중사례관리 \(D\)
- 노동정책: 평가점수 \(R\)가 기준 이하이면 훈련 프로그램 참가 \(D\)

즉 \(R\)는 단순히 결과를 설명하는 공변량이 아니라, **정책의 행정적 문턱을 구현하는 변수**다.

## 임계값(cutoff)은 왜 핵심인가

RDD에서 인과효과는 \(R\) 전체 범위에서가 아니라 cutoff \(c\)에서 식별된다. 왜냐하면 cutoff 바로 주변에서는 제도 규칙상 처치 여부만 달라지고, 다른 잠재적 특성은 부드럽게 이어진다고 보기 때문이다.

잠재결과 함수가 cutoff에서 연속이라면,

\[
\lim_{r\uparrow c}\mathbb{E}[Y(0)\mid R=r]
=
\lim_{r\downarrow c}\mathbb{E}[Y(0)\mid R=r]
\]

그리고 similarly

\[
\lim_{r\uparrow c}\mathbb{E}[Y(1)\mid R=r]
=
\lim_{r\downarrow c}\mathbb{E}[Y(1)\mid R=r]
\]

라고 볼 수 있다. 이때 관측된 결과의 불연속은 처치 때문이라고 해석할 수 있다. 즉 cutoff는 단순한 기준점이 아니라, **식별이 발생하는 지점**이다.

정책평가에서 이 의미는 매우 중요하다. cutoff는 단지 행정 편의상 정한 숫자가 아니라, 연구자가 인과효과를 읽을 수 있는 제도적 경계가 된다. 따라서 좋은 RDD 연구는 단순히 점수 규칙을 소개하는 데서 그치지 않고,

- 왜 그 cutoff가 실제로 배정규칙으로 작동했는지
- cutoff 근방 단위들이 왜 비교 가능하다고 볼 수 있는지
- cutoff에서 동시에 다른 정책이 바뀌지는 않는지

를 함께 설명해야 한다.

## “비슷한 사람끼리 비교”가 여기서는 어떻게 구현되는가

13장의 회귀조정, 매칭, IPW는 공변량 \(X\)를 이용해 처치군과 비교군을 비슷하게 만드는 접근이었다. 반면 RDD는 \(R\)의 아주 좁은 구간에 집중해, cutoff 바로 위와 아래를 비교한다.

이 점에서 RDD의 핵심 직관은 다음과 같다.

- 전체적으로는 서로 많이 다른 집단일 수 있다.
- 하지만 cutoff 바로 근처에서는 제도상 배정 차이만 남고 다른 차이는 매우 작다고 본다.
- 따라서 그 국소 비교를 인과효과로 읽는다.

즉 RDD는 전표본 균형보다 **경계에서의 국소 비교 가능성**을 이용하는 설계다.

## 국소 효과라는 말의 의미

RDD의 추정량은 일반적으로

\[
\tau_{RDD}(c)
\]

처럼 cutoff에서 정의된 효과다. 이는 모집단 전체 ATE와 다를 수 있다. 예를 들어 장학금 cutoff가 90점이라면, RDD가 식별하는 것은 90점 주변 학생들에게 장학금이 미치는 효과이지, 모든 학생에게 장학금을 지급했을 때의 평균 효과가 아니다.

이 점은 정책평가에서 매우 중요하다. 경계 근방의 사람들은 보통 “간당간당하게 자격을 얻거나 잃는” 집단이다. 따라서 RDD는 종종 **정책 경계에서의 한계적 효과**를 보여준다.

## Sharp와 fuzzy는 어디서 갈리는가

RDD의 기본 구조는 sharp와 fuzzy로 나뉜다. sharp RDD에서는 cutoff를 넘는 순간 처치가 기계적으로 바뀐다. 반면 fuzzy RDD에서는 cutoff가 처치확률을 높일 뿐, 실제 순응은 불완전하다.

예를 들어

- “90점 이상이면 반드시 장학금”은 sharp에 가깝고,
- “90점 이상이면 장학금 대상이 되지만 실제 신청·선발 과정이 추가로 개입”하면 fuzzy에 가깝다.

따라서 15.1에서 중요한 것은 배정변수와 cutoff가 설계의 언어를 만든다는 점이고, 15.2와 15.3은 그 구조가 sharp인지 fuzzy인지에 따라 식별과 추정이 어떻게 달라지는지를 보여준다.

## 정책평가에서 자주 생기는 오해

RDD에 대해 자주 생기는 오해는 “기준점이 있으면 자동으로 인과효과를 안다”는 생각이다. 하지만 실제로는 다음을 먼저 확인해야 한다.

- cutoff 근방에서 사람이 점수를 조작할 수 없는가
- cutoff에서 다른 정책도 동시에 바뀌지 않는가
- 배정변수의 측정오차가 작은가
- 충분히 많은 관측치가 cutoff 주변에 존재하는가

즉 배정변수와 cutoff를 아는 것은 시작일 뿐이고, 그 경계가 실제로 비교 가능한 지점을 만들어 주는지를 보여줘야 비로소 RDD가 설득력을 갖는다.

## 사례로 보면 왜 중요한가

복지급여 연구를 생각해 보자. 자산점수 \(R\)가 cutoff 아래면 급여를 받고, 위면 못 받는다고 하자. 전체 가구를 비교하면 빈곤 정도, 노동시장 상태, 가족구성이 많이 달라 인과해석이 어렵다. 하지만 cutoff 바로 아래 가구와 바로 위 가구는 자산점수상 거의 같고, 급여 여부만 다르다고 볼 수 있다. 이때 RDD는 급여의 국소적 효과를 설득력 있게 보여줄 수 있다.

장학금이나 입학정책도 마찬가지다. 전교생 평균 비교는 자기선택과 능력 차이에 취약하지만, cutoff 근처 학생들끼리는 그 차이가 크게 줄어든다.

## 정리

RDD에서 배정변수는 정책 규칙을 수치화한 축이고, cutoff는 처치가 바뀌는 경계이자 식별이 일어나는 지점이다. 따라서 15장을 이해하는 첫 단계는 “어떤 변수를 통제할 것인가”가 아니라, “어떤 배정변수가 어떤 cutoff를 통해 처치를 결정하는가”를 정확히 이해하는 것이다.
